#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * _strcat - function to concatenatestrings
 * @dest: variable to store first string
 * @src: variable to store second string
 * Return: concatenated string
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
