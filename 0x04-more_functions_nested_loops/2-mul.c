#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * mul - function to multiply
 * @a: variable to store first number
 * @b: variable to store second number
 */
int mul(int a, int b)
{

	return (a*b);
}
