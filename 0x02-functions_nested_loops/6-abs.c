#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs Entry point
 * @n: value to be compared
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-1)*n;
	}
	else if ( n == 0)
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
