#include "main.h"
#include <stdio>
#include <strings.h>
#include <string.h>
#include <stdlib.h>
/**
 * _strlen - function to count characters in a string
 * @s: string to be cunted
 * REturn:0
 */

int _strlen(char *s)
{
	printf("%d", strlen(s));

	return (0);
}
