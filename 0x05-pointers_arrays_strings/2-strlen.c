#include "main.h"
#include <string.h>
#include <stdlib.h>
/**
 * _strlen - function to count characters in a string
 * @s: string to be cunted
 * REturn:0
 */

int _strlen(char *s)
{
	char v[] = *s;
	_strlen(v);
	
	return (0);
}
