#include "main.h"
#include <stdio.h>
#include <strings.h>
#include <string.h>
#include <stdlib.h>
/**
 * _strlen - function to count characters in a string
 * @s: string to be cunted
 * REturn: value
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
